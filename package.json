{
  "name": "browser-launcher",
  "version": "1.0.0",
  "description": "Launch browser with extension",
  "main": "launch-browser.js",
  "dependencies": {
    "playwright": "^1.40.0",
    "concurrently": "^8.0.0",
    "@agentdeskai/browser-tools-server": "^1.2.0"
  },
  "scripts": {
    "start:browser": "node launch-browser.js",
    "start:tools": "npx @agentdeskai/browser-tools-server@1.2.0",
    "start": "npm run start:browser && npm run start:tools"
  }
}
